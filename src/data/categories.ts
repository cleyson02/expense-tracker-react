import { Category } from '../types/Category';

export const categories: Category = {
    food: { title: 'Despesa', color: 'blue', expense: true },
    salary: { title: 'Receita', color: 'green', expense: false }
}